require "spec_helper"

RSpec.describe ZachcollinsViewToolDevcamp do
  it "has a version number" do
    expect(ZachcollinsViewToolDevcamp::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
